# 무대 송출 슬라이드 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 2026 BP·WP·AX 경진대회용 무대 송출 슬라이드 2종(심사위원 안내, 시상 발표)을 인터랙티브 단일-폴더 HTML로 만든다.

**Architecture:** 순수 집계·시퀀스 로직(`logic.js`, UMD)과 이벤트 설정(`config.js`)을 분리해 Node로 단위테스트하고, 두 HTML 덱이 classic `<script src>`로 로드한다(빌드 단계 없음, `file://` 오프라인 실행 가능). 시상 덱은 admin과 동일한 중앙값 집계를 복제하고 admin 스냅샷과 parity 테스트로 동일성을 보증한다. 시상 데이터는 Apps Script(JSONP) 라이브 fetch → 메모리 스냅샷.

**Tech Stack:** 바닐라 HTML/CSS/JS(ES5 호환 UMD), Node `node:test`/`node:assert`, `vm`(parity), 벤더 `qrcode-generator`(QR), 손수 만든 canvas confetti, yt-dlp(BGM 1회 추출).

## Global Constraints

- 해상도 1920×1080(16:9), 다크 스테이지 테마: 배경 near-black, 포인트 `--rausch:#FF385C`, 시상 골드 `--gold:#E0A100`, 실버 `--silver:#8A9099`. 폰트 `"Pretendard","맑은 고딕","Malgun Gothic",sans-serif`.
- 두 덱 공통 제어키: `→`/클릭/`Space`=다음(첫 입력=시작+BGM 게이트), `←`=이전, `F`=전체화면, `B`=블랙아웃.
- 단일 폴더 복사로 동작(외부 CDN 런타임 의존 금지). 예외: 시상 덱 라이브 fetch(인터넷), BGM 음원 파일(`assets/bgm/`, gitignore).
- 집계는 admin과 **동일 알고리즘**. 시상 순서: 분야 `WP→BP→AX`, 분야 내 `rank 내림차순`(우수상 먼저 → 대상 마지막).
- 한국어 문장은 마침표로 끝낸다(콜론 종결 금지). 새 소스 첫 줄에 역할 한국어 주석.
- 커밋은 의미 단위. 메시지 끝에 `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- 집계 점수 단위: 심사위원 제출은 분야별 **팀당 총점 0~100**(judge가 1~5×가중치로 환산). 시상 집계는 이 총점의 중앙값.

---

### Task 1: 프로젝트 스캐폴드 + 이벤트 설정

**Files:**
- Create: `package.json`
- Create: `config.js`
- Create: `test/fixtures/admin.template.html` (admin 스냅샷 복사)
- Create: `test/config.test.js`

**Interfaces:**
- Produces: 전역/`module.exports` `CONFIG` 객체 — `{event,teams[6],pmax,categories,categoryReuse,steps,tieRule,awards,apiUrl,judgeUrl,revealOrder,bgm}`.

- [ ] **Step 1: package.json 작성**

```json
{
  "name": "stage-slides-2026",
  "private": true,
  "version": "1.0.0",
  "description": "2026 BP/WP/AX 경진대회 무대 송출 슬라이드",
  "scripts": {
    "test": "node --test"
  }
}
```

- [ ] **Step 2: config.js 작성** (스킬 `config.template.json` 값과 일치 + 덱 키 추가)

```js
// 무대 슬라이드 공유 설정 — 스킬 config.template.json 스냅샷 + 덱 전용 키
var CONFIG = {
  event: {
    title: "2026 BP · WP · AX 경진대회",
    subtitle: "경영기획팀 · 각 항목 1~5점 (가중치 자동 환산)"
  },
  teams: ["중국법인", "태국법인", "HQ", "한국사업부", "이집트법인", "기술연구소"],
  pmax: 5,
  categories: {
    BP: [
      {"n": "위기 인식 및 대응 전략의 적절성", "w": 20},
      {"n": "성과 및 비용 효과성 (정량 성과)", "w": 20},
      {"n": "ERP 기반 데이터 검증", "w": 5},
      {"n": "실행력 및 추진력", "w": 20},
      {"n": "지속가능성 및 확산 가능성", "w": 15},
      {"n": "창의성 및 돌파력", "w": 20}
    ],
    AX: [
      {"n": "생성형 AI 업무적용", "w": 25},
      {"n": "지능형 프로세스 자동화", "w": 30},
      {"n": "데이터 기반 의사결정", "w": 30},
      {"n": "기타 안전분야", "w": 15}
    ]
  },
  categoryReuse: { WP: "BP" },
  steps: [
    { key: "BP", label: "BP", sub: "우수사례" },
    { key: "WP", label: "WP", sub: "개선·반면교사" },
    { key: "AX", label: "AX", sub: "디지털 전환" }
  ],
  tieRule: { byDept: "대표이사", byDeptAlias: "ceo", multiplier: 1.2 },
  awards: {
    default: [
      { rank: 1, prize: "상금 200만원", ttl: "대상" },
      { rank: 2, prize: "상금 100만원", ttl: "우수상" }
    ],
    WP: [ { rank: 1, prize: "상금 100만원", ttl: "대상" } ],
    AX: [
      { rank: 1, prize: "상금 100만원", ttl: "대상" },
      { rank: 2, prize: "상금 50만원", ttl: "우수상" },
      { rank: 3, prize: "상금 50만원", ttl: "우수상" }
    ]
  },
  apiUrl: "PASTE_YOUR_APPS_SCRIPT_URL_HERE",
  judgeUrl: "https://example.invalid/judge.html",
  revealOrder: ["WP", "BP", "AX"],
  bgm: { mode: "file", file: "assets/bgm/bgm.m4a", youtubeId: "U34kLXjdw90", volume: 0.5 }
};
if (typeof module !== "undefined" && module.exports) module.exports = CONFIG;
```

- [ ] **Step 3: admin 스냅샷 복사** (parity 기준; 변경 시 재복사)

Run:
```bash
mkdir -p "test/fixtures"
cp "/c/Users/shins/.claude/skills/scoring-system/templates/admin.template.html" "test/fixtures/admin.template.html"
```
Expected: `test/fixtures/admin.template.html` 존재(약 420줄).

- [ ] **Step 4: config 스모크 테스트 작성**

```js
// config.js 형태 스모크 — 필수 키·팀 수·시상 계약
const test = require("node:test");
const assert = require("node:assert");
const CONFIG = require("../config.js");

test("config: 팀 6개·pmax 5", () => {
  assert.equal(CONFIG.teams.length, 6);
  assert.equal(CONFIG.pmax, 5);
});
test("config: revealOrder == WP,BP,AX", () => {
  assert.deepEqual(CONFIG.revealOrder, ["WP", "BP", "AX"]);
});
test("config: awards.default 존재 + WP/AX override", () => {
  assert.ok(CONFIG.awards.default.length >= 1);
  assert.equal(CONFIG.awards.WP[0].rank, 1);
  assert.equal(CONFIG.awards.AX.length, 3);
});
```

- [ ] **Step 5: 테스트 실행**

Run: `npm test`
Expected: 3 tests PASS.

- [ ] **Step 6: 커밋**

```bash
git add package.json config.js test/config.test.js test/fixtures/admin.template.html
git commit -m "feat: project scaffold + event config + admin snapshot"
```

---

### Task 2: 집계 로직 이식 + admin parity (TDD, 고위험)

**Files:**
- Create: `logic.js`
- Create: `test/parity.test.js`

**Interfaces:**
- Produces: `SCORING.median(arr)`, `SCORING.buildDATA(config, submissions)→{BP,WP,AX:[{n,d,s:number[]}]}`, `SCORING.wscore(config, DATA, cat, teamIndex)→number`, `SCORING.ranking(config, DATA, cat)→[{t,i,med,mean,w}]`(med 내림차순, 동률 시 w 내림차순). admin `templates/admin.template.html`의 `med/buildDATA/wscore/ranking`와 동일 출력.
- Consumes: Task 1 `CONFIG`, `test/fixtures/admin.template.html`.

- [ ] **Step 1: parity 테스트 작성(실패 예정)**

```js
// 시상 집계(logic.js)가 admin 스냅샷의 집계와 동일함을 단언 — 무대 오답 방지
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const vm = require("node:vm");
const CONFIG = require("../config.js");
const SCORING = require("../logic.js");

// admin 스냅샷의 <script>를 CONFIG 주입해 vm으로 실행 → 실제 ranking 확보
function loadAdmin(config) {
  const html = fs.readFileSync(path.join(__dirname, "fixtures", "admin.template.html"), "utf8");
  const m = html.match(/<script>([\s\S]*?)<\/script>/);
  const code = m[1].replace('"__CONFIG__"', JSON.stringify(config));
  const noop = () => {};
  const makeNode = () => ({
    style: {}, dataset: {}, value: "", _html: "",
    classList: { add: noop, remove: noop, toggle: noop, contains: () => false },
    addEventListener: noop, appendChild: noop, removeChild: noop, setAttribute: noop,
    getAttribute: () => "", querySelector: () => makeNode(), querySelectorAll: () => [],
    get innerHTML() { return this._html; }, set innerHTML(v) { this._html = v; }
  });
  const document = {
    getElementById: () => makeNode(), querySelector: () => makeNode(),
    querySelectorAll: () => [], createElement: () => makeNode(), body: makeNode(), addEventListener: noop
  };
  const ctx = { document, window: {}, Date, Math, JSON, console, setTimeout: () => 0, clearTimeout: noop, alert: noop, confirm: () => false };
  vm.createContext(ctx);
  vm.runInContext(code + "\n;", ctx);
  return ctx;
}

// 담합 픽스처: 정상 5명(T0=90,T1=70,그외 50) + 담합 3명(T0=10,T1=100,그외 50)
function collusion(config) {
  const T = config.teams, skill = T[0], coll = T[1];
  const mk = (name, dept, f) => {
    const scores = { BP: {}, WP: {}, AX: {} };
    T.forEach((t) => ["BP", "WP", "AX"].forEach((c) => { scores[c][t] = f(t); }));
    return { name, dept, scores };
  };
  const subs = [];
  for (let i = 0; i < 5; i++) subs.push(mk("정상" + i, T[2], (t) => t === skill ? 90 : t === coll ? 70 : 50));
  for (let i = 0; i < 3; i++) subs.push(mk("담합" + i, T[3], (t) => t === skill ? 10 : t === coll ? 100 : 50));
  return subs;
}

test("parity: ranking 팀 순서·중앙값이 admin과 동일(BP/WP/AX)", () => {
  const subs = collusion(CONFIG);
  const admin = loadAdmin(CONFIG);
  const mineDATA = SCORING.buildDATA(CONFIG, subs);
  for (const cat of ["BP", "WP", "AX"]) {
    admin.DATA = admin.buildDATA(subs);
    const a = admin.ranking(cat);
    const b = SCORING.ranking(CONFIG, mineDATA, cat);
    assert.deepEqual(b.map((r) => r.t), a.map((r) => r.t), cat + " 팀 순서 불일치");
    a.forEach((r, i) => assert.ok(Math.abs(r.med - b[i].med) < 1e-9, cat + " 중앙값 불일치 " + r.t));
  }
});

test("parity: 중앙값 1위=실력팀(담합 차단), 평균 1위=담합팀", () => {
  const subs = collusion(CONFIG);
  const DATA = SCORING.buildDATA(CONFIG, subs);
  const rows = SCORING.ranking(CONFIG, DATA, "BP");
  const byMean = [...rows].sort((x, y) => y.mean - x.mean);
  assert.equal(rows[0].t, CONFIG.teams[0]);
  assert.equal(byMean[0].t, CONFIG.teams[1]);
});

test("median: 홀짝·null 제외", () => {
  assert.equal(SCORING.median([5, 1, 3]), 3);
  assert.equal(SCORING.median([1, 2, 3, 4]), 2.5);
  assert.equal(SCORING.median([null, 4, null, 2]), 3);
});
```

- [ ] **Step 2: 실행해 실패 확인**

Run: `npm test`
Expected: FAIL — `Cannot find module '../logic.js'`.

- [ ] **Step 3: logic.js 집계부 구현** (admin 함수 그대로 이식, 순수화)

```js
// 무대 시상 라이브 집계 — admin.template.html과 동일한 중앙값 집계(이식)
(function (root, factory) {
  if (typeof module === "object" && module.exports) module.exports = factory();
  else root.SCORING = factory();
})(typeof self !== "undefined" ? self : this, function () {
  function median(a) {
    a = a.filter(function (x) { return x != null; }).sort(function (x, y) { return x - y; });
    var m = Math.floor(a.length / 2);
    return a.length ? (a.length % 2 ? a[m] : (a[m - 1] + a[m]) / 2) : 0;
  }
  function mean(a) {
    a = a.filter(function (x) { return x != null; });
    return a.length ? a.reduce(function (s, x) { return s + x; }, 0) / a.length : 0;
  }
  function buildDATA(config, subs) {
    var TEAMS = config.teams, D = { BP: [], WP: [], AX: [] };
    (subs || []).forEach(function (sub) {
      ["BP", "WP", "AX"].forEach(function (cat) {
        var sc = (sub.scores && sub.scores[cat]) || {};
        var s = TEAMS.map(function (t) { return (sc[t] == null ? null : +sc[t]); });
        D[cat].push({ n: sub.name || "(무명)", d: sub.dept || "", s: s });
      });
    });
    return D;
  }
  function wscore(config, DATA, cat, ti) {
    var num = 0, den = 0, TR = config.tieRule;
    DATA[cat].forEach(function (j) {
      var v = j.s[ti]; if (v == null) return;
      var w = (j.d === TR.byDept || j.d === TR.byDeptAlias) ? TR.multiplier : 1;
      num += v * w; den += w;
    });
    return den ? num / den : 0;
  }
  function ranking(config, DATA, cat) {
    var TEAMS = config.teams;
    var rows = TEAMS.map(function (t, i) {
      var col = DATA[cat].map(function (j) { return j.s[i]; });
      return { t: t, i: i, med: median(col), mean: mean(col), w: wscore(config, DATA, cat, i) };
    });
    rows.sort(function (a, b) { return b.med - a.med || b.w - a.w; });
    return rows;
  }
  return { median: median, mean: mean, buildDATA: buildDATA, wscore: wscore, ranking: ranking };
});
```

- [ ] **Step 4: 실행해 통과 확인**

Run: `npm test`
Expected: parity 3 + config 3 = 6 PASS.

- [ ] **Step 5: 커밋**

```bash
git add logic.js test/parity.test.js
git commit -m "feat: median aggregation ported from admin + parity test"
```

---

### Task 3: 시상 매핑(winners) + 리빌 시퀀스(buildScreens) (TDD)

**Files:**
- Modify: `logic.js` (factory 반환 객체에 함수 추가)
- Create: `test/reveal.test.js`

**Interfaces:**
- Produces:
  - `SCORING.revealCats(config)→string[]` (config.revealOrder, 기본 ["WP","BP","AX"]).
  - `SCORING.winners(config, DATA, cat)→[{rank,title,prize,medal,team,med}]` (awards 스펙의 존재하는 rank만, rank 오름차순).
  - `SCORING.buildScreens(config, DATA)→[{type,cat,label,name,rank,title,prize,medal,team,med,isGrand}]` — 분야 revealOrder 순, 분야마다 `cat` 카드 1개 + 각 수상마다 `suspense`+`reveal`(수상은 rank 내림차순=우수상 먼저).
- Consumes: Task 2 `ranking`, Task 1 `CONFIG`.

- [ ] **Step 1: reveal 테스트 작성(실패 예정)**

```js
// 시상 매핑·리빌 시퀀스 — 순서(WP→BP→AX, 우수상→대상)와 화면 수 단언
const test = require("node:test");
const assert = require("node:assert");
const CONFIG = require("../config.js");
const SCORING = require("../logic.js");

// 결정적 더미: 팀별 총점이 명확히 갈리게(중앙값 = 그대로)
function demoDATA() {
  const subs = [];
  for (let i = 0; i < 3; i++) {
    const scores = { BP: {}, WP: {}, AX: {} };
    CONFIG.teams.forEach((t, idx) => ["BP", "WP", "AX"].forEach((c) => { scores[c][t] = 90 - idx * 5; }));
    subs.push({ name: "위원" + i, dept: "심사", scores });
  }
  return SCORING.buildDATA(CONFIG, subs);
}

test("revealCats == WP,BP,AX", () => {
  assert.deepEqual(SCORING.revealCats(CONFIG), ["WP", "BP", "AX"]);
});

test("winners: AX는 rank 1·2·3, 메달 매핑", () => {
  const DATA = demoDATA();
  const w = SCORING.winners(CONFIG, DATA, "AX");
  assert.deepEqual(w.map((x) => x.rank), [1, 2, 3]);
  assert.equal(w[0].medal, "🥇");
  assert.equal(w[1].medal, "🥈");
  assert.equal(w[2].medal, "🏅");
  assert.equal(w[0].team, CONFIG.teams[0]); // 총점 1위
  assert.equal(w[0].prize, "상금 100만원");
});

test("buildScreens: 15화면, WP cat 먼저, 각 분야 수상은 우수상→대상", () => {
  const DATA = demoDATA();
  const s = SCORING.buildScreens(CONFIG, DATA);
  // WP(1수상): cat+2 = 3 / BP(2수상): cat+4 = 5 / AX(3수상): cat+6 = 7  => 15
  assert.equal(s.length, 15);
  assert.equal(s[0].type, "cat");
  assert.equal(s[0].cat, "WP");
  // BP 블록: 첫 reveal은 rank 2(우수상), 마지막 reveal은 rank 1(대상)
  const bp = s.filter((x) => x.cat === "BP" && x.type === "reveal");
  assert.deepEqual(bp.map((x) => x.rank), [2, 1]);
  assert.equal(bp[bp.length - 1].isGrand, true);
});
```

- [ ] **Step 2: 실행해 실패 확인**

Run: `npm test`
Expected: FAIL — `SCORING.revealCats is not a function`.

- [ ] **Step 3: logic.js에 매핑·시퀀스 추가** (factory 내부, `return` 직전에 함수 정의하고 반환 객체에 추가)

```js
  function revealCats(config) {
    return (config.revealOrder && config.revealOrder.length) ? config.revealOrder.slice() : ["WP", "BP", "AX"];
  }
  function medalFor(rank) { return rank === 1 ? "🥇" : rank === 2 ? "🥈" : "🏅"; }
  function winners(config, DATA, cat) {
    var spec = (config.awards[cat] || config.awards.default || []);
    var rows = ranking(config, DATA, cat);
    return spec.map(function (a) {
      var r = rows[a.rank - 1];
      if (!r) return null;
      return { rank: a.rank, title: a.ttl || (a.rank + "등"), prize: a.prize, medal: medalFor(a.rank), team: r.t, med: r.med };
    }).filter(function (x) { return x; });
  }
  function buildScreens(config, DATA) {
    var stepByKey = {};
    config.steps.forEach(function (s) { stepByKey[s.key] = s; });
    var screens = [];
    revealCats(config).forEach(function (cat) {
      var st = stepByKey[cat] || { label: cat, sub: cat };
      screens.push({ type: "cat", cat: cat, label: st.label, name: st.sub || st.label });
      var ws = winners(config, DATA, cat).slice().sort(function (a, b) { return b.rank - a.rank; }); // 우수상 먼저
      ws.forEach(function (w) {
        var base = { cat: cat, label: st.label, name: st.sub || st.label, rank: w.rank, title: w.title, prize: w.prize, medal: w.medal, team: w.team, med: w.med, isGrand: w.rank === 1 };
        screens.push(Object.assign({ type: "suspense" }, base));
        screens.push(Object.assign({ type: "reveal" }, base));
      });
    });
    return screens;
  }
```
그리고 `return { ... }` 객체에 `revealCats: revealCats, winners: winners, buildScreens: buildScreens` 추가.

- [ ] **Step 4: 실행해 통과 확인**

Run: `npm test`
Expected: config 3 + parity 3 + reveal 3 = 9 PASS.

- [ ] **Step 5: 커밋**

```bash
git add logic.js test/reveal.test.js
git commit -m "feat: awards winners mapping + reveal screen sequence"
```

---

### Task 4: 에셋 — QR 라이브러리 벤더 + BGM 추출

**Files:**
- Create: `vendor/qrcode.js` (qrcode-generator 복사)
- Create: `assets/bgm/bgm.m4a` (gitignore; yt-dlp 1회 추출)
- Create: `test/qrcode.test.js`

**Interfaces:**
- Produces: 전역/`require` `qrcode` 함수 — `qrcode(type, ecc)` → `.addData/.make/.createSvgTag(cellSize,margin)`.

- [ ] **Step 1: qrcode-generator 벤더**

Run:
```bash
npm i qrcode-generator@1.4.4
cp "node_modules/qrcode-generator/qrcode.js" "vendor/qrcode.js"
```
Expected: `vendor/qrcode.js` 존재(`node_modules`는 .gitignore에 이미 포함).

- [ ] **Step 2: QR 스모크 테스트 작성**

```js
// 벤더 QR 라이브러리가 인코딩되는지 스모크
const test = require("node:test");
const assert = require("node:assert");
const qrcode = require("../vendor/qrcode.js");

test("qrcode: URL 인코딩 → SVG 태그 생성", () => {
  const qr = qrcode(0, "M");
  qr.addData("https://example.invalid/judge.html");
  qr.make();
  const svg = qr.createSvgTag(6, 0);
  assert.ok(qr.getModuleCount() > 0);
  assert.ok(/<svg/.test(svg));
});
```

- [ ] **Step 3: 실행해 통과 확인**

Run: `npm test`
Expected: 직전 9 + qrcode 1 = 10 PASS.

- [ ] **Step 4: BGM 추출** (지정 트랙 → 로컬 음원; 실패 시 수동 대체)

Run:
```bash
mkdir -p "assets/bgm"
yt-dlp -x --audio-format m4a -o "assets/bgm/bgm.%(ext)s" "https://www.youtube.com/watch?v=U34kLXjdw90"
```
Expected: `assets/bgm/bgm.m4a` 생성(약 수 MB). 만약 `yt-dlp` 미설치면 `pip install -U yt-dlp` 또는 `winget install yt-dlp` 후 재시도. 그래도 안 되면 운영자가 차분한 음원 파일을 `assets/bgm/bgm.m4a`로 직접 둔다(README에 명시). 이 파일은 gitignore라 커밋되지 않는다.

- [ ] **Step 5: 커밋** (BGM 음원 제외)

```bash
git add vendor/qrcode.js test/qrcode.test.js
git commit -m "chore: vendor qrcode-generator + smoke test"
```

---

### Task 5: 심사위원 안내 덱 (`guidance.html`)

**Files:**
- Create: `guidance.html`

**Interfaces:**
- Consumes: `config.js`(`CONFIG.judgeUrl`, `CONFIG.bgm`, `CONFIG.steps`), `vendor/qrcode.js`, `logic.js`(미사용 가능), `assets/bgm/bgm.m4a`.

- [ ] **Step 1: guidance.html 작성** (전체 코드)

```html
<!DOCTYPE html>
<html lang="ko">
<!-- 심사위원 안내 무대 슬라이드 — QR·접속주소·입력 4스텝 회전·차분 BGM -->
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>심사 평가 안내</title>
<style>
  :root{ --rausch:#FF385C; --ink:#F4F4F5; --muted:#9aa0a6; --bg:#0E0E12; --card:#17171D; --line:#2A2A33; }
  *{box-sizing:border-box;margin:0;padding:0;}
  html,body{height:100%;}
  body{font-family:"Pretendard","맑은 고딕","Malgun Gothic",sans-serif;background:var(--bg);color:var(--ink);
    overflow:hidden;}
  /* 16:9 무대 캔버스: 1920x1080 기준으로 스케일 */
  #stage{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;}
  #canvas{width:1920px;height:1080px;transform-origin:center center;display:grid;
    grid-template-columns:780px 1fr;gap:64px;padding:96px 110px;align-content:center;}
  .left{display:flex;flex-direction:column;align-items:center;justify-content:center;
    background:var(--card);border:1px solid var(--line);border-radius:28px;padding:56px 48px;}
  .left .label{font-size:26px;font-weight:700;color:var(--rausch);letter-spacing:.02em;margin-bottom:28px;}
  #qr{width:440px;height:440px;background:#fff;border-radius:20px;padding:24px;display:flex;align-items:center;justify-content:center;}
  #qr svg{width:100%;height:100%;}
  .url{margin-top:30px;font-size:30px;font-weight:700;word-break:break-all;text-align:center;color:#fff;}
  .url small{display:block;font-size:20px;color:var(--muted);font-weight:500;margin-top:8px;}
  .right{display:flex;flex-direction:column;justify-content:center;}
  .htitle{font-size:30px;color:var(--muted);font-weight:600;}
  .h1{font-size:72px;font-weight:800;letter-spacing:-.03em;line-height:1.1;margin:10px 0 12px;}
  .h1 b{color:var(--rausch);}
  .recusal{display:inline-flex;align-items:center;gap:10px;font-size:24px;font-weight:600;color:#cfe9d8;
    background:rgba(26,127,85,.18);border:1px solid rgba(26,127,85,.5);padding:14px 22px;border-radius:999px;margin-top:8px;}
  .steps{margin-top:46px;display:flex;flex-direction:column;gap:18px;}
  .step{display:flex;align-items:center;gap:26px;padding:26px 30px;border-radius:20px;border:1px solid var(--line);
    background:#121218;opacity:.35;transition:opacity .4s, transform .4s, border-color .4s;}
  .step.on{opacity:1;border-color:var(--rausch);transform:translateX(8px);background:#1b1b22;}
  .step .no{flex:0 0 auto;width:62px;height:62px;border-radius:16px;display:grid;place-items:center;
    font-size:30px;font-weight:800;background:#000;color:var(--rausch);border:1.5px solid var(--line);}
  .step.on .no{background:var(--rausch);color:#fff;border-color:var(--rausch);}
  .step .tx{font-size:34px;font-weight:700;}
  .step .tx small{display:block;font-size:22px;color:var(--muted);font-weight:500;margin-top:4px;}
  .hint{position:fixed;bottom:26px;left:0;right:0;text-align:center;color:var(--muted);font-size:18px;opacity:.7;}
  #start{position:fixed;inset:0;background:#0E0E12;display:flex;align-items:center;justify-content:center;
    flex-direction:column;gap:20px;cursor:pointer;z-index:20;}
  #start h2{font-size:48px;font-weight:800;} #start p{font-size:24px;color:var(--muted);}
  .blackout{position:fixed;inset:0;background:#000;z-index:30;display:none;}
  .blackout.on{display:block;}
</style>
</head>
<body>
<div id="start"><h2>심사 평가 안내</h2><p>화면을 클릭하거나 Space를 눌러 시작</p></div>
<div id="stage"><div id="canvas">
  <div class="left">
    <div class="label">📱 심사 평가 페이지 접속</div>
    <div id="qr"></div>
    <div class="url" id="url"></div>
  </div>
  <div class="right">
    <div class="htitle">지금부터</div>
    <div class="h1"><b>심사 평가</b> 시간입니다</div>
    <div class="recusal">✓ 본인 소속 팀은 자동으로 평가 대상에서 제외됩니다</div>
    <div class="steps" id="steps"></div>
  </div>
</div></div>
<div class="hint">→ / Space 다음 · ← 이전 · F 전체화면 · B 블랙아웃</div>
<div class="blackout" id="blackout"></div>
<audio id="bgm" loop></audio>

<script src="config.js"></script>
<script src="vendor/qrcode.js"></script>
<script>
// 입력 4스텝 정의(judge 흐름을 본뜬 미니어처 텍스트)
var STEPS = [
  { no: "1", t: "평가 분야 선택", s: "BP · WP · AX 중 현재 발표 분야" },
  { no: "2", t: "평가 대상 팀 선택", s: "법인/사업부 카드에서 선택 (본인 소속 제외)" },
  { no: "3", t: "항목별 1~5점", s: "각 평가 항목을 1~5점 버튼으로 채점" },
  { no: "4", t: "제출", s: "제출 후에도 평가 시간 내 수정 가능" }
];
var stepsEl = document.getElementById("steps");
stepsEl.innerHTML = STEPS.map(function (s) {
  return "<div class='step'><div class='no'>" + s.no + "</div><div class='tx'>" + s.t + "<small>" + s.s + "</small></div></div>";
}).join("");
var stepNodes = stepsEl.querySelectorAll(".step");

// QR + URL
(function () {
  var qr = qrcode(0, "M");
  qr.addData(CONFIG.judgeUrl);
  qr.make();
  document.getElementById("qr").innerHTML = qr.createSvgTag(6, 0);
  var u = CONFIG.judgeUrl.replace(/^https?:\/\//, "");
  document.getElementById("url").innerHTML = u + "<small>휴대폰 카메라로 QR을 스캔하세요</small>";
})();

// 16:9 스케일 맞춤
function fit() {
  var c = document.getElementById("canvas");
  var s = Math.min(window.innerWidth / 1920, window.innerHeight / 1080);
  c.style.transform = "scale(" + s + ")";
}
window.addEventListener("resize", fit); fit();

// 스텝 자동 회전
var cur = 0;
function showStep(i) {
  cur = (i + stepNodes.length) % stepNodes.length;
  stepNodes.forEach(function (n, k) { n.classList.toggle("on", k === cur); });
}
var timer = null;
function autorotate() { clearInterval(timer); timer = setInterval(function () { showStep(cur + 1); }, 3500); }

// 시작 게이트(BGM 자동재생 정책 대응)
var started = false;
function start() {
  if (started) return; started = true;
  document.getElementById("start").style.display = "none";
  var bgm = document.getElementById("bgm");
  if (CONFIG.bgm && CONFIG.bgm.mode === "file") {
    bgm.src = CONFIG.bgm.file; bgm.volume = CONFIG.bgm.volume != null ? CONFIG.bgm.volume : 0.5;
    bgm.play().catch(function () {});
  }
  showStep(0); autorotate();
}
document.getElementById("start").addEventListener("click", start);

// 제어키
var blackout = document.getElementById("blackout");
document.addEventListener("keydown", function (e) {
  if (e.key === " " || e.key === "ArrowRight") { e.preventDefault(); if (!started) { start(); return; } showStep(cur + 1); autorotate(); }
  else if (e.key === "ArrowLeft") { showStep(cur - 1); autorotate(); }
  else if (e.key.toLowerCase() === "f") { if (!document.fullscreenElement) document.documentElement.requestFullscreen(); else document.exitFullscreen(); }
  else if (e.key.toLowerCase() === "b") { blackout.classList.toggle("on"); }
});
document.getElementById("stage").addEventListener("click", function () { if (started) { showStep(cur + 1); autorotate(); } });
</script>
</body>
</html>
```

- [ ] **Step 2: 정적 구조 테스트 작성**

```js
// guidance.html이 필수 요소·스크립트를 포함하는지 정적 점검
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const html = fs.readFileSync(path.join(__dirname, "..", "guidance.html"), "utf8");

test("guidance: config·qrcode 로드 + 핵심 요소", () => {
  assert.ok(html.includes('src="config.js"'));
  assert.ok(html.includes('src="vendor/qrcode.js"'));
  assert.ok(html.includes('id="qr"'));
  assert.ok(html.includes('id="bgm"'));
  assert.ok(html.includes('id="steps"'));
});
```
파일명: `test/guidance.test.js`.

- [ ] **Step 3: 실행해 통과 확인**

Run: `npm test`
Expected: 직전 10 + guidance 1 = 11 PASS.

- [ ] **Step 4: 수동 확인** (브라우저)

`guidance.html`을 크롬으로 연다(또는 `npx serve .` 후 접속). 확인: Space로 시작→BGM 재생, QR이 그려지고 폰으로 스캔 시 `CONFIG.judgeUrl`이 열림(현재 더미면 더미 주소), 4스텝이 3.5초마다 회전, F 전체화면, B 블랙아웃. 1920×1080에서 잘림 없음.

- [ ] **Step 5: 커밋**

```bash
git add guidance.html test/guidance.test.js
git commit -m "feat: judge guidance stage deck (QR + steps + BGM)"
```

---

### Task 6: 시상 발표 덱 (`awards.html`)

**Files:**
- Create: `awards.html`

**Interfaces:**
- Consumes: `config.js`, `logic.js`(`buildDATA`,`buildScreens`), `assets/bgm/bgm.m4a`. Apps Script `CONFIG.apiUrl`(JSONP `action=get`).

- [ ] **Step 1: awards.html 작성** (전체 코드)

```html
<!DOCTYPE html>
<html lang="ko">
<!-- 시상 발표 무대 슬라이드 — 라이브 집계(JSONP)→스냅샷, WP→BP→AX 우수상→대상 극적 리빌 -->
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>시상 발표</title>
<style>
  :root{ --rausch:#FF385C; --gold:#E0A100; --silver:#8A9099; --ink:#F4F4F5; --muted:#9aa0a6; --bg:#08080B; }
  *{box-sizing:border-box;margin:0;padding:0;}
  body{font-family:"Pretendard","맑은 고딕","Malgun Gothic",sans-serif;background:var(--bg);color:var(--ink);overflow:hidden;}
  #stage{position:fixed;inset:0;display:flex;align-items:center;justify-content:center;}
  #canvas{position:relative;width:1920px;height:1080px;transform-origin:center;overflow:hidden;}
  .scr{position:absolute;inset:0;display:flex;flex-direction:column;align-items:center;justify-content:center;
    opacity:0;transition:opacity .5s;text-align:center;padding:0 120px;}
  .scr.on{opacity:1;}
  /* 분야 카드 */
  .cat-eyebrow{font-size:34px;font-weight:700;color:var(--muted);letter-spacing:.08em;}
  .cat-k{font-size:200px;font-weight:900;letter-spacing:-.04em;color:var(--rausch);line-height:1;margin:8px 0;}
  .cat-name{font-size:52px;font-weight:700;}
  /* 서스펜스 */
  .su-cat{font-size:40px;color:var(--muted);font-weight:700;}
  .su-title{font-size:96px;font-weight:900;margin:14px 0 28px;}
  .su-dots{font-size:120px;letter-spacing:.1em;color:var(--rausch);animation:pulse 1.1s infinite;}
  @keyframes pulse{0%,100%{opacity:.3}50%{opacity:1}}
  /* 리빌 */
  .rv-medal{font-size:180px;line-height:1;filter:drop-shadow(0 10px 30px rgba(0,0,0,.5));animation:pop .6s cubic-bezier(.2,1.4,.4,1);}
  .rv-rank{font-size:40px;font-weight:800;letter-spacing:.04em;margin-top:6px;}
  .rv-team{font-size:140px;font-weight:900;letter-spacing:-.03em;margin:6px 0 8px;animation:slam .6s cubic-bezier(.15,1.5,.4,1);}
  .rv-score{font-size:40px;color:var(--muted);} .rv-score b{color:#fff;font-size:52px;}
  .rv-prize{margin-top:30px;font-size:42px;font-weight:800;color:#fff;background:var(--rausch);
    padding:18px 40px;border-radius:999px;display:inline-block;}
  .gold .rv-rank{color:var(--gold);} .gold .rv-prize{background:linear-gradient(135deg,#F0C24B,var(--gold));color:#1a1400;}
  .silver .rv-rank{color:var(--silver);}
  @keyframes pop{0%{transform:scale(0);opacity:0}100%{transform:scale(1);opacity:1}}
  @keyframes slam{0%{transform:scale(1.6);opacity:0;filter:blur(8px)}100%{transform:scale(1);opacity:1;filter:blur(0)}}
  #confetti{position:absolute;inset:0;pointer-events:none;}
  #status{position:fixed;top:18px;left:18px;font-size:15px;color:var(--muted);background:rgba(0,0,0,.5);
    padding:6px 12px;border-radius:8px;z-index:40;}
  .hint{position:fixed;bottom:20px;left:0;right:0;text-align:center;color:var(--muted);font-size:16px;opacity:.55;}
  #start{position:fixed;inset:0;background:var(--bg);display:flex;align-items:center;justify-content:center;
    flex-direction:column;gap:18px;cursor:pointer;z-index:50;}
  #start h2{font-size:56px;font-weight:900;} #start p{font-size:22px;color:var(--muted);}
  .blackout{position:fixed;inset:0;background:#000;z-index:60;display:none;} .blackout.on{display:block;}
</style>
</head>
<body>
<div id="start"><h2>🏆 시상식</h2><p>결과를 불러오는 중...</p></div>
<div id="status">—</div>
<div id="stage"><div id="canvas"><canvas id="confetti" width="1920" height="1080"></canvas></div></div>
<div class="hint">→ / Space 다음 · ← 이전 · R 결과 새로고침 · F 전체화면 · B 블랙아웃</div>
<div class="blackout" id="blackout"></div>
<audio id="bgm" loop></audio>

<script src="config.js"></script>
<script src="logic.js"></script>
<script>
// 데모 더미(?demo=1): API 없이 리허설용 — 팀 총점이 명확히 갈리는 합성 제출
var DEMO_SUBS = (function () {
  var subs = [];
  for (var i = 0; i < 3; i++) {
    var scores = { BP: {}, WP: {}, AX: {} };
    CONFIG.teams.forEach(function (t, idx) { ["BP", "WP", "AX"].forEach(function (c) { scores[c][t] = 92 - idx * 6 - (c === "AX" ? idx : 0); }); });
    subs.push({ name: "위원" + i, dept: "심사", scores: scores });
  }
  return subs;
})();

var canvas = document.getElementById("canvas");
var SCREENS = [], idx = 0, started = false;

function fit() {
  var s = Math.min(window.innerWidth / 1920, window.innerHeight / 1080);
  canvas.style.transform = "scale(" + s + ")";
}
window.addEventListener("resize", fit); fit();

function setStatus(t) { document.getElementById("status").textContent = t; }

// 화면 1개 렌더 → DOM 요소 반환
function renderScreen(s) {
  var el = document.createElement("div");
  el.className = "scr" + (s.type === "reveal" ? (s.rank === 1 ? " gold" : s.rank === 2 ? " silver" : "") : "");
  if (s.type === "cat") {
    el.innerHTML = "<div class='cat-eyebrow'>시상 부문</div><div class='cat-k'>" + s.label + "</div><div class='cat-name'>" + s.name + "</div>";
  } else if (s.type === "suspense") {
    el.innerHTML = "<div class='su-cat'>" + s.label + " · " + s.title + "</div><div class='su-title'>과연 그 주인공은…</div><div class='su-dots'>● ● ●</div>";
  } else {
    el.innerHTML = "<div class='rv-medal'>" + s.medal + "</div><div class='rv-rank'>" + s.label + " · " + s.title + "</div>"
      + "<div class='rv-team'>" + s.team + "</div><div class='rv-score'>최종 <b>" + s.med.toFixed(1) + "</b> / 100</div>"
      + "<div class='rv-prize'>" + s.prize + "</div>";
  }
  return el;
}

function show(i) {
  if (!SCREENS.length) return;
  idx = Math.max(0, Math.min(SCREENS.length - 1, i));
  var old = canvas.querySelector(".scr.on");
  if (old) { old.classList.remove("on"); setTimeout(function () { if (old.parentNode) old.parentNode.removeChild(old); }, 500); }
  var s = SCREENS[idx];
  var el = renderScreen(s);
  canvas.appendChild(el);
  void el.offsetWidth; // reflow
  el.classList.add("on");
  if (s.type === "reveal" && s.isGrand) burstConfetti();
  setStatus((idx + 1) + " / " + SCREENS.length + " · " + s.cat + (s.type === "reveal" ? " " + s.title : ""));
}

// JSONP (admin과 동일 방식)
function jsonp(params) {
  return new Promise(function (res, rej) {
    var cb = "cb_" + Date.now() + Math.floor(Math.random() * 10000);
    var sc = document.createElement("script");
    var timer = setTimeout(function () { cleanup(); rej("시간 초과"); }, 15000);
    function cleanup() { clearTimeout(timer); delete window[cb]; if (sc.parentNode) sc.parentNode.removeChild(sc); }
    window[cb] = function (d) { cleanup(); res(d); };
    sc.onerror = function () { cleanup(); rej("네트워크 오류"); };
    sc.src = CONFIG.apiUrl + "?callback=" + cb + "&" + params;
    document.body.appendChild(sc);
  });
}

function buildFrom(subs) {
  var DATA = SCORING.buildDATA(CONFIG, subs);
  SCREENS = SCORING.buildScreens(CONFIG, DATA);
  idx = 0;
}

function loadResults() {
  var demo = new URLSearchParams(location.search).has("demo");
  if (demo) { buildFrom(DEMO_SUBS); document.getElementById("start").querySelector("p").textContent = "데모 데이터 · 클릭/Space로 시작"; setStatus("데모 모드 · " + SCREENS.length + "화면"); return Promise.resolve(); }
  return jsonp("action=get").then(function (d) {
    if (!d.ok) throw d.error || "오류";
    buildFrom(d.submissions || []);
    document.getElementById("start").querySelector("p").textContent = "준비 완료 · 클릭/Space로 시작";
    setStatus("결과 동기화 " + new Date().toLocaleTimeString("ko-KR") + " · " + SCREENS.length + "화면");
  }).catch(function (e) {
    document.getElementById("start").querySelector("p").textContent = "결과 로드 실패: " + e + " — R로 재시도";
    setStatus("로드 실패: " + e);
  });
}

// 손수 만든 컨페티
var cx = document.getElementById("confetti").getContext("2d");
var parts = [];
function burstConfetti() {
  for (var i = 0; i < 160; i++) parts.push({ x: 960, y: 380, vx: (Math.random() - .5) * 22, vy: Math.random() * -18 - 6,
    g: .5, c: ["#FF385C", "#E0A100", "#fff", "#FF7A93"][i % 4], s: 6 + Math.random() * 8, r: Math.random() * 6 });
}
(function loop() {
  cx.clearRect(0, 0, 1920, 1080);
  for (var i = parts.length - 1; i >= 0; i--) {
    var p = parts[i]; p.vy += p.g; p.x += p.vx; p.y += p.vy; p.r += .2;
    cx.save(); cx.translate(p.x, p.y); cx.rotate(p.r); cx.fillStyle = p.c; cx.fillRect(-p.s / 2, -p.s / 2, p.s, p.s); cx.restore();
    if (p.y > 1140) parts.splice(i, 1);
  }
  requestAnimationFrame(loop);
})();

function start() {
  if (started || !SCREENS.length) return; started = true;
  document.getElementById("start").style.display = "none";
  var bgm = document.getElementById("bgm");
  if (CONFIG.bgm && CONFIG.bgm.mode === "file") { bgm.src = CONFIG.bgm.file; bgm.volume = CONFIG.bgm.volume != null ? CONFIG.bgm.volume : 0.5; bgm.play().catch(function () {}); }
  show(0);
}

var blackout = document.getElementById("blackout");
document.addEventListener("keydown", function (e) {
  var k = e.key.toLowerCase();
  if (e.key === " " || e.key === "ArrowRight") { e.preventDefault(); if (!started) { start(); return; } show(idx + 1); }
  else if (e.key === "ArrowLeft") { show(idx - 1); }
  else if (k === "r") { started = false; SCREENS = []; document.getElementById("start").style.display = "flex"; document.getElementById("start").querySelector("p").textContent = "결과 새로고침 중..."; loadResults(); }
  else if (k === "f") { if (!document.fullscreenElement) document.documentElement.requestFullscreen(); else document.exitFullscreen(); }
  else if (k === "b") { blackout.classList.toggle("on"); }
});
document.getElementById("start").addEventListener("click", start);
document.getElementById("stage").addEventListener("click", function () { if (started) show(idx + 1); });

loadResults();
</script>
</body>
</html>
```

- [ ] **Step 2: 정적 구조 테스트 작성**

```js
// awards.html이 logic·config 로드, 라이브 fetch, 데모 모드, 컨페티를 포함하는지 정적 점검
const test = require("node:test");
const assert = require("node:assert");
const fs = require("node:fs");
const path = require("node:path");
const html = fs.readFileSync(path.join(__dirname, "..", "awards.html"), "utf8");

test("awards: 핵심 배선 포함", () => {
  assert.ok(html.includes('src="config.js"'));
  assert.ok(html.includes('src="logic.js"'));
  assert.ok(html.includes("SCORING.buildScreens"));
  assert.ok(html.includes("action=get"));        // 라이브 fetch
  assert.ok(html.includes('has("demo")'));         // 데모 모드
  assert.ok(html.includes('id="confetti"'));
});
```
파일명: `test/awards.test.js`.

- [ ] **Step 3: 실행해 통과 확인**

Run: `npm test`
Expected: 직전 11 + awards 1 = 12 PASS.

- [ ] **Step 4: 수동 리허설** (데모 모드)

`awards.html?demo=1`을 크롬으로 연다(`npx serve .` 후 `localhost:3000/awards.html?demo=1` 권장). 확인: 클릭/Space 시작→BGM, 화면이 `WP 분야카드→(서스펜스→리빌)→BP…→AX…` 순서, 분야 내 우수상 먼저 대상 마지막, 대상에서 컨페티, ← 뒤로, R 재로드, F/B 동작. 15화면이 끝까지 진행되고 잘림 없음.

- [ ] **Step 5: 커밋**

```bash
git add awards.html test/awards.test.js
git commit -m "feat: awards reveal stage deck (live aggregation + dramatic reveal)"
```

---

### Task 7: 운영 README + 최종 검증

**Files:**
- Create: `README.md`

- [ ] **Step 1: README 작성**

```markdown
# 2026 BP·WP·AX 경진대회 무대 슬라이드

무대 송출용 인터랙티브 HTML 2종.

- `guidance.html` — 심사위원 안내(평가 시간 송출): QR·접속주소·입력 4스텝·BGM.
- `awards.html` — 시상 발표(사회자 송출): WP→BP→AX, 우수상→대상, 라이브 집계 + 극적 리빌.

## 실행
- 가장 안정적: 폴더에서 `npx serve .` 후 크롬으로 `http://localhost:3000/guidance.html`, `…/awards.html` 접속.
- 또는 파일을 크롬으로 직접 열기(`file://`). 시상 라이브 집계는 인터넷 필요.
- 무대에선 `F`로 전체화면.

## 제어키
→ / Space 다음 · ← 이전 · F 전체화면 · B 블랙아웃 · (시상) R 결과 새로고침.

## 행사 전 설정(`config.js`)
- `judgeUrl` — 심사 평가 페이지 공개 배포 주소(안내 QR이 가리킴). GitHub Pages 등.
- `apiUrl` — Apps Script `/exec` 주소(시상 라이브 집계 fetch). 스킬 setup-guide 참고.
- 두 값을 넣고 저장하면 QR·집계가 실제로 동작한다.

## 시상 리허설
- API 없이 점검: `awards.html?demo=1` (합성 데이터로 전체 리빌 확인).
- 실제 행사: 심사 종료 후 `awards.html`을 열면 라이브 집계로 승자가 채워진다. 네트워크가 불안하면 무대 직전 한 번 열어 스냅샷을 잡고 그대로 진행한다(끊겨도 리빌은 메모리로 동작). 갱신은 `R`.

## BGM
`assets/bgm/bgm.m4a`를 재생한다. 파일이 없으면 차분한 음원을 그 경로에 두면 된다(저작권은 내부 사용 기준으로 운영자가 판단). 유튜브 직접 재생이 필요하면 `config.js`의 `bgm.mode`를 `"youtube"`로 두는 옵션이 있으나 인터넷·광고 리스크로 비상용.

## 집계 정합성
시상 집계는 관리자(admin)와 **동일한 중앙값 알고리즘**이다. `npm test`의 parity 테스트가 admin 스냅샷과 동일 결과임을 보증한다. 스킬의 집계가 바뀌면 `test/fixtures/admin.template.html`을 다시 복사하고 `npm test`로 재확인한다.
```

- [ ] **Step 2: 전체 테스트 최종 실행**

Run: `npm test`
Expected: 12 PASS, 0 FAIL.

- [ ] **Step 3: 데크 수동 최종 점검**

`guidance.html`과 `awards.html?demo=1`을 크롬 전체화면(1920×1080)으로 끝까지 본다. 안내(QR·회전·BGM), 시상(순서·컨페티·BGM·블랙아웃) 모두 정상이고 잘림 없음.

- [ ] **Step 4: 커밋**

```bash
git add README.md
git commit -m "docs: operations README + final verification"
```

---

## Self-Review

- **스펙 커버리지**: 안내 덱(QR·주소·입력안내·BGM)=Task5 / 시상 덱(WP→BP→AX·우수상→대상·극적)=Task3+Task6 / 라이브 집계+스냅샷=Task6 / admin 동일 집계+parity=Task2 / BGM 로컬=Task4 / 다크 16:9 테마·제어키=Task5·6 / 전제조건(judgeUrl·apiUrl)=config+README. 누락 없음.
- **플레이스홀더**: 모든 코드 스텝에 실제 코드 포함. `apiUrl`/`judgeUrl`은 placeholder 문자열이지만 이는 행사 전 운영자 입력값(스펙 7절의 의도된 전제)이며 더미로 개발 가능.
- **타입 일관성**: `buildDATA(config,subs)`·`ranking(config,DATA,cat)`·`winners(config,DATA,cat)`·`buildScreens(config,DATA)` 시그니처가 테스트·HTML 호출과 일치. `SCREENS` 항목 필드(type/cat/label/name/rank/title/prize/medal/team/med/isGrand)가 `renderScreen`과 일치.
